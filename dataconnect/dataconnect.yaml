specVersion: "v1beta"
serviceId: "bloopfishapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bloopfishapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
